https://www.youtube.com/watch?v=IRbtu9mIli4&list=PLJq4rR8tWINyOrvwxbIjRvgTtoyuuEfSz&index=6
AVEVA™ System Platform – OMI Web Client Overview

生成中文教程，格式 md ，含引用圖片 images 目錄，圖片為依序取得


This video explains the OMI Web Client, a browser-based view application users can access and interact with, as if they were using a regular view application. It covers the essential capabilities and benefits of developing and deploying an OMI Web Client. 

A demonstration of the OMI Web Client is also provided.


影片 AVEVA™ System Platform – OMI Web Client Overview 的完整字幕內容與時間軸整理如下：

    [00:03] The OMI web client essentially supports a web-based view app that users can access and interact with as if they were using a regular view app.

    [00:11] Users simply need to launch a web browser, such as the latest versions of Google Chrome or Microsoft Edge, and enter the URL address for the OMI web client.

    [00:29] Let's take a step back and discuss the capabilities and the benefits of the web client.

    [00:39] First, it is easily accessible and resembles the desktop client, so users do not require additional training to use it.

    [00:48] At the same time, there is security in place to ensure that only authorized individuals can access it.

    [00:58] Additionally, no special configuration is needed in the Galaxy system except for using the web view engine instead of the view engine.

    [01:07] When a view app is deployed on the web view engine, it is converted into web-based components for consumption through a web browser.

    [01:17] Moreover, the OMI web client lists all available OMI view apps and, just like a regular view app, it displays graphics, alarm visualizations, trend visualizations, etc.

    [01:36] The following slide shows the architecture of the web client framework. To run the OMI web client, the installation of Bootstrap is required.

    [01:45] A WinPlatform should be deployed, and instead of deploying the view engine which supports regular view apps, the web view engine should be used to support the OMI web client.

    [02:05] Above the web view engine, one or more view app instances can be deployed and run.

    [02:14] On a remote node, users can launch a web browser and connect to the node where the web view engine is deployed to retrieve graphic and data information.

    [02:27] This is what the OMI web client looks like. First, I can select the view app I'd like to view and even control.

    [02:39] I can easily navigate through the view app and, if needed, make changes such as opening and closing the valves, provided I have the appropriate permissions.

    [02:48] Thanks for watching.

