https://learningacademy.aveva.com/learn/video/aveva-system-platform-creating-a-new-project-1?client=customer2024




[19:22:45] In this video we will explain how to create a new galaxy and describe the templates that are available when launching the system platform IDE the backstage area is displayed the backstage area is used to create a new galaxy in the specified galaxy repository note this is where we will configure and launch our galaxy on this window we can click the new tab to view only the configuration options for creating a new galaxy or we can stay on the home tab for our demonstration we will stay on the home tab if the server name displayed here is not the galaxy repository server where we want to create the galaxy we would either enter the correct server name or click the down arrow and select it from the list in this case our server name for the galaxy repository is correct when creating a galaxy we use the galaxy template to indicate what will be the default content of the new galaxy the following galaxy templates are available out of the box the default galaxy is the recommended starting point for creating a new galaxy it contains default screen profiles templates a basic equipment mode and an in touch OMI view app example that introduces key features the blank galaxy template creates a baseline galaxy that contains only base template objects the in touch base template creates a galaxy that includes only the objects and graphics needed for tag based managed in touch applications the reactor demo template creates a galaxy with a version of the reactor demo based on a tag based managed in touch application other galaxy types can be made available using backups of other galaxies this allows the distribution of standards such as preconfigure objects graphics and system level settings so let's create a new galaxy first we will click the start button and click on system platform ID in the backstage view we will click blank galaxy in the creative galaxy dialogue box galaxy name field we will enter training galaxy in the template name field we will leave the default_empty.
[19:22:45] cab file selected as this option is automatically populated from when we selected the blank galaxy tile if we leave the connect to this galaxy checkbox check the ID will open when the button is clicked so now we will click create the create galaxy dialogue box opens and shows the progress of the galaxy being created since we left the connect to this galaxy checkbox checked the IDE opens automatically after the galaxy is created after a few moments the system platform IDE opens allowing us to begin configuring our galaxy

